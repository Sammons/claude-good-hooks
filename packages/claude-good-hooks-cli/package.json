{
  "name": "@sammons/claude-good-hooks",
  "version": "1.0.0",
  "description": "CLI for managing Claude Code hooks",
  "bin": {
    "claude-good-hooks": "./dist/index.mjs"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean --shims --tsconfig tsconfig.build.json",
    "test": "vitest run",
    "dev": "tsup src/index.ts --format esm --watch",
    "lint": "eslint src --ext .ts,.tsx",
    "lint:fix": "eslint src --ext .ts,.tsx --fix"
  },
  "keywords": [
    "claude",
    "hooks",
    "cli"
  ],
  "author": "Sammons Software LLC",
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0"
  },
  "peerDependencies": {
    "@sammons/claude-good-hooks-types": "^1.0.0"
  },
  "dependencies": {
    "@sammons/claude-good-hooks-types": "workspace:*",
    "@sammons/claude-good-hooks-factories": "workspace:*",
    "chalk": "^5.3.0"
  },
  "devDependencies": {
    "@sammons/dirty-good-claude-hook": "workspace:*",
    "@types/node": "^22.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.0.0",
    "vitest": "^2.0.0"
  }
}
