{
  "name": "@sammons/claude-good-hooks",
  "version": "1.0.0",
  "description": "CLI for managing Claude Code hooks",
  "bin": {
    "claude-good-hooks": "./dist/index.js"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean --shims",
    "test": "vitest run",
    "dev": "tsup src/index.ts --format esm --watch"
  },
  "keywords": ["claude", "hooks", "cli"],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "@sammons/claude-good-hooks-types": "workspace:*",
    "commander": "^12.0.0",
    "chalk": "^5.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.0.0",
    "vitest": "^2.0.0"
  }
}